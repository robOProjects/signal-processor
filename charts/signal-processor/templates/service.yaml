apiVersion: v1
kind: Service
metadata:
  name: {{ include "signal-processor.fullname" . }}
  labels:
    {{- include "signal-processor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "signal-processor.selectorLabels" . | nindent 4 }}
