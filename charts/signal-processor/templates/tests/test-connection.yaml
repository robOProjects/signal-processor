apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "signal-processor.fullname" . }}-test-connection"
  labels:
    {{- include "signal-processor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "signal-processor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
